---
import { m } from '../paraglide/messages.js';
import { APP_URL } from '../constants/urls';

const locale = Astro.currentLocale ?? 'fr';

const links = [
  { href: `${APP_URL}/${locale}/legal`, label: m.footer_legal() },
  { href: `${APP_URL}/${locale}/terms`, label: m.footer_terms() },
  { href: `${APP_URL}/${locale}/privacy`, label: m.footer_privacy() },
  { href: `${APP_URL}/${locale}/login`, label: m.footer_login() },
];
---

<footer class="border-border-token relative z-1 border-t px-7 py-9">
  <div
    class="text-ink-4 mx-auto flex max-w-(--content-max) flex-wrap items-center justify-between gap-4.5 text-[13.5px]"
  >
    <span>{m.footer_copyright()}</span>
    <nav class="flex flex-wrap gap-5.5">
      {
        links.map((link) => (
          <a href={link.href} class="text-ink-3 hover:text-ink transition-colors">
            {link.label}
          </a>
        ))
      }
    </nav>
  </div>
</footer>
